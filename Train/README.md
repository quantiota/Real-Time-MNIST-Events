# N-MNIST Train Set

Each folder (0-9) contains binary event recordings of that digit class.
Only digit 0 is uploaded to the repository as an example.

To download all digits, run:
```bash
python download.py
```
